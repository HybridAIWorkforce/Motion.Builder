# Contributing to Motion.Builder

## Setup

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000)

## Guidelines

- Run `npm run lint` before committing
- Follow existing component patterns
- Use TypeScript for all new code
